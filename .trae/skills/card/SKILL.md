---
name: "card"
description: "从笔记库提取 500 字以内的内容生成精美卡片，用于手机分享，结尾注明来源；若来源为网页则附加链接二维码，最终将 PNG 卡片保存到系统相册（macOS 照片 App）。支持一次输入多个问题（列表形式）批量生成多张卡片。当用户要求「做成卡片」「生成分享卡片」「卡片分享」时使用。"
---

# 卡片技能

## 功能描述

卡片技能是指在卡片中添加技能，用于在卡片中执行特定的操作。

卡片技能的流程如下：
1. 根据输入的描述从笔记库中检索相关笔记，确定内容来源
2. 提取 500 字以内的精华内容
3. 转化为卡片，用于手机分享
4. 内容最后给出来源的人名或者书名；若来源为某网页，将网页链接生成二维码附加在卡片末尾
5. 将卡片渲染为 PNG 图片输出
6. 将生成的 PNG 卡片保存到系统相册（macOS 照片 App）
7. 中间产物 HTML 不保留，完成后删除，只保留相册中的 PNG

## 操作步骤

### 多问题批量处理

1. 若用户一次输入多个问题（列表形式，如 `1. ... 2. ... 3. ...`），将每个问题视为一张独立卡片，逐个依次处理
2. 对每个问题独立执行以下完整流程（检索→排版→渲染→导入→清理），问题之间互不干扰
3. 每张卡片独立命名：`<该问题主题>-card.png`；若文件名重复可追加序号
4. 全部卡片生成完毕后，统一汇报每张卡片的主题、尺寸与相册导入结果

### 单张卡片流程

1. 理解用户输入，明确用户想要提取的内容主题
2. 在笔记库中检索与该主题相关的内容，确定内容来源（人名/书名/网页），选取最精华的部分（500 字以内）
3. 将内容排版为手机分享卡片格式
4. 在内容末尾注明来源（人名或书名）；若来源是网页，记录其链接（网页链接通常在笔记头部或原文中）
5. 生成来源网页的二维码并附加在卡片末尾：
   - 使用二维码生成服务（如 `https://api.qrserver.com/v1/create-qr-code/?size=220x220&data=<URL 编码后的链接>`）生成二维码图片
   - 将二维码嵌入卡片 HTML，置于来源信息旁边或下方
   - 若无网页来源（如书籍/人名），跳过此步骤
6. 将卡片渲染为 PNG 图片：用浏览器打开卡片 HTML 后对卡片区域截图保存为 PNG，或使用 html2canvas 等工具将卡片 DOM 导出为 PNG 图片；图片宽度固定 1080px，高度按内容动态调整（详见「动态高度实现」）
7. 将生成的 PNG 卡片保存到系统相册：使用 `osascript -e 'tell application "Photos" to import POSIX file "<PNG 绝对路径>"'` 导入 macOS 照片 App；若命令失败，改用 `open -a Photos "<PNG 路径>"` 手动导入
8. 删除临时 HTML 文件，只保留相册中的 PNG

## 保存到系统相册

- 保存目标：macOS 照片 App（系统相册）
- 导入方式（macOS）：
  - `osascript -e 'tell application "Photos" to import POSIX file "/path/to/card.png"'` —— 直接导入照片图库
  - 备选：`open -a Photos "/path/to/card.png"` 打开照片 App 后手动拖入/导入
- 导入后删除本地临时 PNG 与 HTML，只保留系统相册中的成品卡片

## PNG 输出要求

1. 最终交付给用户的成品卡片保存在系统相册（macOS 照片 App），HTML 与本地临时 PNG 完成后删除
2. 图片宽度固定 1080px，高度由卡片内容动态决定（不使用固定 1440 高度、不裁剪、不留白），保证文字清晰可读
3. 图片中需保留卡片完整内容与末尾来源信息；若来源为网页，二维码必须出现在图片末尾

## 动态高度实现

1. 卡片 HTML 中不写死高度：`html, body { width: 1080px; }`，`.card` 不设 `height`，高度由内容自然撑开
2. 渲染 PNG 时先让浏览器以较大窗口高度（如 1440px）截图，再用图片处理工具（如 Python PIL）裁剪掉底部多余空白，得到贴合内容高度的 PNG
3. 或使用支持整页截图的工具（如 Playwright 的 `page.screenshot(full_page=True)`）直接输出内容高度的 PNG

## UI 规范

1. 默认用细体平方（PingFang SC Light）
2. 卡片内间距为 1em
3. 文字颜色固定不变：
   - 正文：`#4a3f2f`
   - 标题：`#6b5a3e`
   - 来源：`#8a7a5c`
4. 卡片背景主色调每次根据内容主题决定一种适合的颜色：
   - 主题联想配色的示例：古典/史诗→米黄、海洋→浅蓝、历史→浅褐、哲思→浅灰绿、宗教→浅驼、自然→浅青等
   - 所有配色都必须为浅淡色（高明度、低饱和度），并叠加做旧效果（暗角、噪点、斑点），保持纸张质感
5. 卡片背景色略微做旧，有做旧效果

## 主题配色示例

| 内容主题 | 建议底色 | 参考色值 |
|----------|----------|----------|
| 古典史诗 / 神话 | 米黄 | `#efe7d7` |
| 海洋 / 漂泊 | 浅雾蓝 | `#dfe7e8` |
| 历史 / 战争 | 浅褐 | `#e8ddd0` |
| 哲思 / 沉思 | 浅灰绿 | `#e2e6dc` |
| 宗教 / 命运 | 浅驼 | `#ece3d3` |
| 自然 / 田园 | 浅青 | `#dde8e0` |
| 暖情 / 人间 | 浅杏 | `#f0e0d2` |

## 示例

输入：「我最近在读《活着》，帮我做一张关于福贵命运的卡片」

输出：一张排版精美的 PNG 卡片图片（保存到系统相册），包含《活着》中关于福贵命运的精华摘录（500 字以内），底部注明「余华《活着》」。

输入（多问题批量）：
```
1. 为什么奥德赛要漂流这么久？
2. 奥德修斯具体做了什么得罪了海神？
3. 为什么神们决定摧毁特洛伊？
```

输出：按列表顺序依次生成 3 张独立卡片（每张均为 1080px 宽、高度按内容自适应、按主题配色、含来源与二维码），全部导入系统相册后统一汇报结果。
